name: number_pad
description: A new Flutter package project.
version: 0.0.6
homepage: https://github.com/Ellie-erp/number_pad

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
flutter:
